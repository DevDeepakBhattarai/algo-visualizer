type Bar = {
  value: Number;
  id: string;
  color: string;
};

type Algorithms =
  | "merge"
  | "bubble"
  | "count"
  | "quick"
  | "insertion"
  | "selection";

type Sound = "ding" | "windchime" | "scale";
